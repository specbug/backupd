# GENERATED by 'hostd sync'. Do not edit.
#
# Every job below is declared in an app's own deploy.yml under 'backup'.
# To change one, edit that file, commit it, and re-run 'hostd sync'.
# Source paths are volume-relative there and rooted at /sources/<app> here.

jobs:
  - name: odyssey-db
    type: sqlite
    source: /sources/odyssey/odyssey.db
    destination: odyssey/db/odyssey.db.gz
  - name: odyssey-uploads
    type: sync
    source: /sources/odyssey/uploads/
    destination: odyssey/uploads/
  - name: draw-scenes
    type: sync
    source: /sources/draw/scenes/
    destination: draw/scenes/
  - name: draw-files
    type: sync
    source: /sources/draw/files/
    destination: draw/files/
